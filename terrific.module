<?php

use Drupal\terrific\Adapter\ContextProvider;
use Drupal\terrific\Factory\ContextFactory;

/**
 * Implements hook_preprocess_HOOK().
 */
function terrific_preprocess_node(&$variables) {
//  $variables[ContextProvider::TERRIFIC_ARRAY_KEY] = (new ContextFactory)->createFromNode($variables['node']);
//  var_dump($variables[ContextProvider::TERRIFIC_ARRAY_KEY]->links);
//  exit;
}
